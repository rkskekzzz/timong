import React from 'react';
import Styled from './SignedCalendar.styled';

const SignedCalendar = () => {
  return <Styled.SignedCalendar>SignedCalendar</Styled.SignedCalendar>;
};

export default SignedCalendar;
