import styled from 'styled-components';

const List = styled.div`
  padding: 25px 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 25px;
`;

const Styled = { List };

export default Styled;
