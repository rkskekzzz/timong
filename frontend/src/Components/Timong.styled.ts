import styled from 'styled-components';

const Body = styled.div`
  max-width: 400px;
  display: flex;
  flex-direction: column;
  margin: 0 auto;
  margin-top: 53px;
`;

const Styled = { Body };

export default Styled;
